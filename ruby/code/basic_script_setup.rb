#!/usr/bin/env ruby

puts "This is a basic ruby script set up"
puts "To run you can just do 'ruby basic_script_setup.rb'"
puts "You can set it up as an executable, add a 'shebang' (#!/usr/bin/ruby
) to the top of the file and then run 'chmod +x basic_script_setup.rb'"
puts "And then run with './basic_script_setup'"
puts "(Hmm, the executable isn't working??)"
